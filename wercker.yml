box: debian
build:
  steps:
    - script:
        name: echo
        code: |
          echo "This is wercker box for terraform!!"
deploy:
  steps:
    - script:
        name: exec terraform plan
        code: |
          sh ./terraform/aws/check_env.sh
