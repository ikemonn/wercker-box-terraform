box: debian
build:
  steps:
    - script:
        name: echo
        code: |
          echo "This is wercker box for terraform!!"
          pwd
          ls -la
    - script:
        name: exec terraform plan
        code: |
          pwd
          ls -la
          sh ./terraform/check_env.sh
