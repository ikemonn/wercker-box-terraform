box: wercker/box-ubuntu12.04-webessentials
build:
  steps:
    - script:
        name: echo
        code: |
          echo "This is wercker box for terraform!!"
          pwd
          ls -la
    - script:
        name: exec terraform plan
        code: |
          pwd
          ls -la
          ls -l /bin/sh
          which git
          bash ./terraform/check_env.sh
